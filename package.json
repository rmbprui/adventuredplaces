{
  "name": "adventuredplaces",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "contributors": [
    "Rui Pereira <rmbp.rui@gmail.com>"
  ],
  "workspaces": [
    "./backend",
    "./frontend"
  ],
  "scripts": {
    "start": "yarn client",
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "server": "yarn --cwd backend start",
    "client": "yarn --cwd frontend start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false yarn --cwd frontend && yarn --cwd frontend build"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
